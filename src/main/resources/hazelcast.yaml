hazelcast:
  cluster-name: springhow
  jet:
    enabled: true
  network:
    join:
      multicast:
        enabled: false
      kubernetes:
        enabled: true
        service-name: your-service-name
        namespace: your-namespace